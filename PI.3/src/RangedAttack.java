import java.awt.Graphics2D;
import java.awt.image.BufferedImage;

/**
 * 
 */

/**
 * @author romulo
 *
 */
public class RangedAttack {

	/**
	 * For ranged weapons only.
	 * 
	 * @param g2d
	 * @param spriteSheet
	 * @param rangedWeapon
	 * @param shot
	 */
	void drawRangedAttackAnimation(Graphics2D g2d, BufferedImage spriteSheet, BufferedImage rangedWeapon,
			BufferedImage shot) {
	}

	/**
	 * For spells only.
	 * 
	 * @param g2d
	 * @param spriteSheet
	 * @param shot
	 */
	void drawRangedAttackAnimation(Graphics2D g2d, BufferedImage spriteSheet, BufferedImage shot) {
	}
}
